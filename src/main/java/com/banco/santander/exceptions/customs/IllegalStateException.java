package com.banco.santander.exceptions.customs;

public class IllegalStateException extends Exception {
    public IllegalStateException(final String errorMessage) {
        super(errorMessage);
    }
}
